{
  "name": "redone-site",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "npm run sync:images && next dev",
    "clean": "rm -rf .next",
    "sync:images": "node scripts/sync-static-images.mjs",
    "sync:init-notes": "node scripts/sync-static-images.mjs --init-notes",
    "normalize-images": "node scripts/normalize-image-colors.mjs",
    "prebuild": "npm run sync:images",
    "build": "next build",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "next": "^14.0.0",
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/react": "^18.0.0",
    "@types/react-dom": "^18.0.0",
    "autoprefixer": "^10.0.0",
    "eslint": "^8.0.0",
    "eslint-config-next": "^14.0.0",
    "postcss": "^8.0.0",
    "sharp": "^0.34.5",
    "tailwindcss": "^3.0.0",
    "typescript": "^5.0.0"
  }
}
